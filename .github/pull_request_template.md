## Summary

What does this PR do?

## Checklist

- [ ] `nix develop --command cargo test --workspace --all-targets` passes
- [ ] `nix develop --command cargo clippy --workspace -- -D warnings` is clean
- [ ] `nix develop -c cargo doc --workspace --no-deps` has no warnings
- [ ] Doc comments added/updated for public API changes
- [ ] Tests added/updated for behavioral changes
